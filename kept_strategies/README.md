# Kept Strategies

Every strategy that passes the configured goals and improves the current best is
exported here by `autoresearch.py`.

Each kept strategy gets its own folder:

```text
001_12.3456_return/
  strategy.py
  README.md
```

The per-strategy README includes full project settings, strategy parameters, and
result metrics.
